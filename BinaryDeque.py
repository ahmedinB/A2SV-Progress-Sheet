def BinaryDeque(binary=100, target=1, n=3):
    steps=0
    while True:
        print("Trying: ", bin(target)[2:])
        if int(0 + binary) == bin(target):
            return steps
        # if


BinaryDeque()